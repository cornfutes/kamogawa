{{define "javascript"}}
<script>
let $d = document,
    rm = (l, c) => l.classList.remove(c),
    ad = (l, c) => l.classList.add(c),
     $ = l => $d.getElementById(l),
     pp = e => e.preventDefault(),
    isK = e => e.code == 'Enter',
     _w = e => (el, f) => el && el.addEventListener(e, f),
     wc = _w('click'),
     wt = _w('keyup'),
     xy = (el, fn) => (wc(el, fn), wt(el, e => isK(e) && (pp(e), fn()))),
     jj = 0,
     q = '',
    $m = $d.getElementsByTagName('main')[0],
    $i = $('s-i'),
    $l = $('logo'),
    $t = $('session-nav'),
    $s = $('search-box'),
     s = q =>  window.location.href = `/search?q=${encodeURI(q)}`,
    fo = _ => { jj=1; rm($s, '👻'); ad($t, '👻'); ad($m, '🌃'); ad($l, '🌃'); if(q && q.length > 0) $i.value= q; $i.focus(); },
  defo = _ => { jj=0; ad($s, '👻'); rm($t, '👻'); rm($m, '🌃'); rm($l, '🌃');                       $i.value='';             };

xy($('search-start'), fo);
wt($d, e => e.code == 'Slash' && (pp(e), fo()))

wt($i, e => {
  if(jj) {
    if (isK(e)) pp(e), s($('s-i').value);
    else if(e.code == 'Escape') pp(e), defo();
    else if(e.code == 'Tab') pp(e);
  }
});
wc($('search-cancel'), defo);

// Not critical
console.log("%c Hello Y = f=>(x=>x(x))(x=>f(y=>x(x)(y)))", "font-size:24px;background:#67b26f;background:-webkit-linear-gradient(to right, #67b26f, #4ca2cd); background: linear-gradient(to right, #67b26f, #4ca2cd);color:white;text-align:center;padding:10px 15px;width:100%;border-radius:20px;");
const lnl = $d.querySelectorAll('#left-nav li > a'),  yy = x => x.href.split('?')[0];
lnl && lnl.forEach(el => yy(el) == yy(window.location) && ad(el.parentElement, 'active'));

{{if eq .IsSearch "yes" }}
 let            f = ['all', 'substring', 'lastmonth'],
            elSoq = $('search-original-query'),
              $r  = $('s-b-r'), 
                r = $("query"),
              $re = $('s-re'),
               $h = $('s-h'),
              val = e => e.target.value,
            reset = _ => { 
                      elSoq.innerText = '', ad($r, '🥷'); 
                      rm($h, '⚡'); ad($re, '⚡'); 
                    };
o = q = '{{.Query}}';

sqb = 0, xy($('s-mod'), e => ((sqb = !sqb) ? rm : ad)($('search-hero'), 'search-hero-hidden'));

wc($r, _ => r.value = o, reset());

function change(e, i) { 
  f[i] = val(e)
  let tk = [];
  if(f[0] != "all"      ) tk.push("provider:" + f[0]);
  if(f[1] != "substring") tk.push(    "algo:" + f[1]);
  if(f[2] != "lastmonth") tk.push(    "time:" + f[2]);
  if(tk.length) {
    q = [r.value.split(":::")[0].trim(), ":::", ...tk].join(" ").trim();
    if (q == o) {
      r.value = o;
      reset();
    } else {
      r.value = q;
      ad($h, '⚡');
      elSoq.innerText = o;
      rm($re, '⚡');
      rm($r, '🥷');
    }
  } else {
    r.value = r.value.split(":::")[0].trim();
    if(r.value == o) reset();
  }
}
wt(r, e => {
  pp(e);
  if(isK(e)) s(val(e));
  else if(val(e) == o) q = val(e), reset();
  else {
    q = val(e);
    ad($h, '⚡');
    elSoq.innerText = o;
    rm($re, '⚡');
    rm($r, '🥷');
  }
});
wc($re, e => q != o && (pp(e), s(q)));
wt($re, e => isK(e) && q != o && pp(e) && s(q));
{{end}}
</script>
{{end}}